<x-layouts.app title="Deals in Himmafushi | Local & Visitor Offers" description="Browse current deals from Himmafushi businesses and guesthouses."><section class="page-hero"><div class="page-shell"><p class="eyebrow">Local offers</p><h1>Deals in Himmafushi</h1><p>Offers for locals, residents and international guests, published directly by island businesses.</p></div></section><section class="section"><div class="page-shell"><div class="deal-tabs"><span>All</span><span>Locals & Residents</span><span>International Guests</span></div><div class="card-grid">@forelse($deals as $deal)<article class="listing-card {{ $deal->guesthouse ? 'listing-card-clickable' : '' }}"><div class="listing-image"><span>{{ $deal->customer_type === 'all' ? 'Himmafushi deal' : ucfirst($deal->customer_type).' offer' }}</span></div><div class="listing-body"><p class="eyebrow">{{ $deal->customer_type === 'all' ? 'All visitors' : ucfirst($deal->customer_type) }}</p><h3>{{ $deal->title }}</h3><p>{{ $deal->description }}</p>@if($deal->deal_price)<div class="price-row"><span>{{ $deal->currency }} {{ number_format($deal->deal_price, 0) }}</span>@if($deal->original_price)<del>{{ $deal->currency }} {{ number_format($deal->original_price, 0) }}</del>@endif</div>@endif@if($deal->guesthouse)<a class="text-link card-primary-link" href="{{ route('guesthouses.show', $deal->guesthouse) }}" aria-label="View {{ $deal->guesthouse->name }}">View guesthouse</a>@endif</div></article>@empty<x-empty-state title="No active deals yet" message="When Himmafushi businesses publish offers, they will appear here." />@endforelse</div><div class="pagination">{{ $deals->links() }}</div></div></section></x-layouts.app>
