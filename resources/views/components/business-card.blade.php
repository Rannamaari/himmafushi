@props(['business'])
<article class="listing-card"><div class="listing-image">@if($business->image)<img src="{{ asset('storage/'.$business->image) }}" alt="{{ $business->name }}" loading="lazy">@else<span>{{ $business->category->name }}</span>@endif</div><div class="listing-body">@if($business->featured)<p class="eyebrow">Featured</p>@endif<h3>{{ $business->name }}</h3><p>{{ $business->short_description ?: 'Discover this local Himmafushi business.' }}</p><div class="card-meta">{{ $business->address ?: 'Himmafushi' }} @if($business->price_range) · {{ $business->price_range }} @endif</div><a class="text-link" href="{{ route($business->category->slug === 'restaurant' ? 'restaurants.show' : ($business->category->slug === 'shop' ? 'shops.show' : ($business->category->slug === 'wellness' ? 'wellness.show' : 'barbers.show')), $business) }}">View details</a></div></article>
