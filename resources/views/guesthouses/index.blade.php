<x-layouts.app title="Guesthouses in Himmafushi | Places to Stay" description="Browse active guesthouses in Himmafushi and request the best available rate."><section class="page-hero"><div class="page-shell"><p class="eyebrow">Island stays</p><h1>Stay in Himmafushi</h1><p>From budget-friendly stays to comfortable island escapes.</p></div></section><section class="section"><div class="page-shell"><div class="card-grid">@forelse($guesthouses as $guesthouse)<article class="listing-card"><div class="listing-image">@if($guesthouse->image)<img src="{{ asset('storage/'.$guesthouse->image) }}" alt="{{ $guesthouse->name }}" loading="lazy">@else<span>Himmafushi stay</span>@endif</div><div class="listing-body">@if($guesthouse->featured)<p class="eyebrow">Featured</p>@endif<h2>{{ $guesthouse->name }}</h2><p>{{ $guesthouse->description ?: 'Request a stay and receive the best available rate for your dates.' }}</p><div class="card-meta">{{ $guesthouse->address ?: 'Himmafushi' }}</div><div class="price-row">@if($guesthouse->local_rate_from)<span>Local MVR {{ number_format($guesthouse->local_rate_from, 0) }}</span>@endif @if($guesthouse->tourist_rate_from)<span>Guest USD {{ number_format($guesthouse->tourist_rate_from, 0) }}</span>@endif @if(! $guesthouse->local_rate_from && ! $guesthouse->tourist_rate_from)<span>Request Rate</span>@endif</div><a class="text-link" href="{{ route('guesthouses.show', $guesthouse) }}">View Guesthouse</a></div></article>@empty<x-empty-state title="Guesthouses are being added" message="Check back soon for Himmafushi accommodation options." />@endforelse</div></div></section></x-layouts.app>
